version: "2"
sql:
  - engine: "postgresql"
    queries: "query/url.sql"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/storage/postgres/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true